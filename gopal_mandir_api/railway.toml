[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./gopal_mandir_api"
healthcheckPath = "/api/temple-info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
